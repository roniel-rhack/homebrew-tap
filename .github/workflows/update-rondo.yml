name: Update rondo formula

on:
  repository_dispatch:
    types: [rondo-release]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to update (e.g., 0.1.0)"
        required: true

jobs:
  update-formula:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Get version
        id: version
        env:
          DISPATCH_VERSION: ${{ github.event.client_payload.version }}
          INPUT_VERSION: ${{ github.event.inputs.version }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [ "$EVENT_NAME" = "repository_dispatch" ]; then
            VERSION="$DISPATCH_VERSION"
          else
            VERSION="$INPUT_VERSION"
          fi
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Download checksums
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          curl -sL "https://github.com/roniel-rhack/rondo/releases/download/v${VERSION}/checksums.txt" -o checksums.txt
          cat checksums.txt

      - name: Update formula
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          SHA_ARM64=$(grep "darwin-arm64" checksums.txt | awk '{print $1}')
          SHA_AMD64=$(grep "darwin-amd64" checksums.txt | awk '{print $1}')
          SHA_LINUX=$(grep "linux-amd64" checksums.txt | awk '{print $1}')

          echo "Updating to version $VERSION"
          echo "SHA256 darwin-arm64: $SHA_ARM64"
          echo "SHA256 darwin-amd64: $SHA_AMD64"
          echo "SHA256 linux-amd64: $SHA_LINUX"

          sed -i "s/version \".*\"/version \"$VERSION\"/" Formula/rondo.rb

          sed -i "/on_arm do/{n;n;s/sha256 \".*\"/sha256 \"$SHA_ARM64\"/}" Formula/rondo.rb
          sed -i "/on_intel do/{n;n;s/sha256 \".*\"/sha256 \"$SHA_AMD64\"/}" Formula/rondo.rb
          sed -i "/on_linux do/{n;n;s/sha256 \".*\"/sha256 \"$SHA_LINUX\"/}" Formula/rondo.rb

          cat Formula/rondo.rb

      - name: Commit and push
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/rondo.rb
          git commit -m "Update rondo to v${VERSION}"
          git push
